
class Note:
    def __init__(self, length, pitch)
        self.length = length
        self.pitch = pitch

    def __str__(self):
        return "length: " + self.length + "\n" + "pitch: " + self.pitch + "\n"